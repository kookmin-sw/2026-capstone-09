spring:
  ai:
    mcp:
      server:
        name: flowmeet-mcp-server
        version: 1.0.0
        type: SYNC
        protocol: SSE
        sse-endpoint: /sse
        sse-message-endpoint: /mcp/message

server:
  port: 8082